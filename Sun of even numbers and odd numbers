#include<stdio.h>
main()
{
int i=1,oddsum=0,evensum=0;
while(i<=10)
{
if(i%2==0)
{
evensum=evensum+i;
}
else
{
oddsum=oddsum+i;
}
i+=1;
}
printf("sum of all odd numbers is %d\n",oddsum);
printf("sum of all even numbers is %d\n",evensum);
}
